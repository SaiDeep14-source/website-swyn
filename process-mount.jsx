import React from 'react';
import ReactDOM from 'react-dom/client';
import ProcessTimeline from './ProcessTimeline.jsx';

ReactDOM.createRoot(document.getElementById('process-root')).render(
  <React.StrictMode>
    <ProcessTimeline />
  </React.StrictMode>
);
